# Backend server
PORT=4000
FRONTEND_URL=http://localhost:5173

# Postgres connection (use DATABASE_URL or individual settings)
# DATABASE_URL=postgres://username:password@localhost:5432/regaloshop
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=regaloshop

# AWS Aurora / SSL configuration
DB_SSL=false
# DB_SSL_STRICT=true

# Pool tuning (opcional)
# DB_POOL_MAX=10
# DB_POOL_IDLE=10000
# DB_STATEMENT_TIMEOUT=30000
